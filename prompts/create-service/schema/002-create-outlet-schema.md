# Variable Definitions

- `schema_name`: outlet
- `columns`: ["id","userId", "name", "address", "businessType", "logo", "tax"]
- `primary_key`: id

# Schema Creation Command

add schema with name `outlet` the columns only contains ["id","userId","name","address","businessType","logo","tax"] where id as pk
